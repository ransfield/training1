import React from 'react'
import DebugInfo from './DebugInfo'

export default function SessionList({ sessions }) {
    return (
        <div>
            {/* <h2>Sessions</h2> */}
            {/* <DebugInfo value={sessions}></DebugInfo> */}
            <div>
                {/* {sessions.map(s => <li>{s.date} --{s.time}</li>)} */}
            </div>
        </div>
    )
}
